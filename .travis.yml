language:
  python

os:
  - linux

matrix:
  include:
    - python: 3.6
      dist: xenial
      sudo: false
    - python: 3.7
      dist: xenial
      sudo: false
    - python: 3.8
      dist: xenial
      sudo: false

install:
  - pip install -r requirements.txt
  - pip install tox-travis

script:
  - tox
